const str1 = "Naveen Singh Junglan BA Pass"

// console.log(str1);

// console.log(typeof (str1)); //To check type of the data..

// console.log(str1.length);

// console.log(str1.anchor("naveen")); //to make it as an <anchor> tag, takes an arg

// console.log(str1.at(5)); //returns the character that is at specified index no, takes an arg

// console.log(str1.big()); //used to make it as <big> tag..

// console.log(str1.blink()); //used to make it as <blink> tag..

// console.log(str1.bold()); //used to make it as <b> tag..

// console.log(str1.charAt(5)); //returns the character that is at specified index no

// console.log(str1.charCodeAt(5)); //returns the character code (ASCII table) that is at specified index no

// console.log(str1.codePointAt(5)); //returns the character code (ASCII table) that is at specified index no


const str2 = " Tinku Junglan"
// console.log(str1.concat(str2)); //used to concate the two strings

// console.log(str1.constructor); // [Function: String]

// console.log(str1.endsWith("s")); // takes an arg and returns true if condition is suticefied

// console.log(str1.fontcolor("red")); // takes an arg and returns string with <font color='specified color name'> tag

// console.log(str1.fontsize(5)); // takes an arg(number) and returns string with <font size='specified size'> tag

// console.log(str1.includes("BA")); // takes an arg(string) and returns true if given string is present at any position

// console.log(str1.indexOf("B")); // takes an arg(string) and returns index number

// console.log(str1.indexOf("B")); // takes an arg(string) and returns index number

// console.log(str2.repeat(3)); // takes an arg(number) and returns a new string which contains the specified number of copies of this string

// console.log(str1.replace('BA', 'BCA')); // takes arg(replace, replaceWith) works only with a word(if we want to replace all the words then we are to use replaceAll()) and returns a new string which word(s) replace the specified with which word(s) of this string

// console.log(str1.search('N')); // takes an arg(search) and returns index no of the specified charactor

// console.log(str1.slice(4, 10)); // takes arg(start, end) and returns string of the given values.

// console.log(str1.split(" ")); // takes a pattern and divides this string into an ordered list of substrings by searching for the pattern

// console.log(str1.startsWith("s")); // takes an arg(str) and returns true if condition is suticefied.

// console.log(str1.strike()); // strikes the given string

// console.log(str1.sub()); // <sub> tag the given string

// console.log(str1.substr(4, 10)); // takes arg(start, length) and returns string of the given values.

// console.log(str1.substring(4, 10)); // takes arg(start, end) and returns string of the given values. Same as slice

console.log(str1.toLocaleLowerCase()); // used to convert into lower case
console.log(str1.toLowerCase()); // used to convert into lower case
console.log(str1.toLocaleUpperCase()); // used to convert into upper case
console.log(str1.toUpperCase()); // used to convert into upper case